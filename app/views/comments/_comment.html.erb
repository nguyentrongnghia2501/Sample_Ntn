<li>

  <% if comment.id %>
     <%= comment.user.name %>:
     <%= comment.body %></br>
     <%= time_ago_in_words(comment.created_at) %>
     <%= link_to "Delete", comment_url(comment), Method: :delete, data: {confirm: "You sure?"} if current_user == comment.user%>     </br></br>
   <% end %>

</li>
